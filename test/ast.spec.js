'use strict';
const assert = require('assert');
const ast = require('../ast');

describe('AST functions', function() {

  describe('getNodeType()', function() {
    xit('can determine node types', function() {
    });
  });

  describe('normalize()', function() {
    xit('normalizes nodes', function() {
    });
  });

  describe('walk()', function() {
    xit('can walk an AST', function() {
    });
  });

});
